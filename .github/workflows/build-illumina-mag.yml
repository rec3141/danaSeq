# DEPRECATED: This workflow referenced illumina_mag/ which has been renamed to
# illumina_assembly/. See build-illumina-assembly.yml instead.
name: "[DEPRECATED] Build illumina_mag image"
on:
  workflow_dispatch:
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use build-illumina-assembly.yml instead."
