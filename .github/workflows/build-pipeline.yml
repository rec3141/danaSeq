# DEPRECATED: This workflow referenced nanopore_mag/ which has been split into
# nanopore_assembly/ and mag_analysis/. See:
#   - build-nanopore-assembly.yml
#   - build-mag-analysis.yml
#   - build-illumina-assembly.yml
name: "[DEPRECATED] Build pipeline image"
on:
  workflow_dispatch:
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use build-nanopore-assembly.yml or build-mag-analysis.yml instead."
