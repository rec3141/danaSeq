# Pre-built local conda environments (Docker builds its own)
conda-envs/

# Nextflow work and output directories
work/
test_results/
results/
results_*/
.nextflow/
.nextflow.log*

# Downloaded databases (large, user mounts separately)
databases/

# Docker context bloat
*.log

# The image installs these itself (npm ci); shipping the host's copy would bloat
# the build context and then be overwritten.
viz/node_modules/
viz/dist/
